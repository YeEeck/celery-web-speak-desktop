# 07 — Web 浮层渲染页面

**What to build:** 主仓库新增独立 Vue 页面（多页构建入口）：渲染语音状态快照——每个参与者一行（头像、显示名、静音/聋图标），行块样式与 Web UI 一致（图标用同一图标集，头像初始逻辑一致），按配置应用说话/未说话整行不透明度，未连接语音时页面完全空白，不显示频道名。页面经壳的浮层 preload 通道接收状态与配置（初始拉取、订阅更新、配置推送）。页面自身不处理窗口几何（壳负责）。浏览器内以 mock 通道数据可完整验证渲染。

**Blocked by:** 05 — 桌面壳协议 2：配置通道、几何应用与远程浮层页

**Status:** resolved

- [ ] 独立入口构建成功，部署路径可访问
- [ ] 快照渲染：行块、头像、名字、静音/聋图标与 Web UI 一致
- [ ] 按配置应用两档不透明度；空态完全空白
- [ ] 浏览器 mock 数据渲染验证（含小写首字母大写、代理对头像字）
- [ ] 经壳通道初始拉取与订阅更新可用
