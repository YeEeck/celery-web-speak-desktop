# 05 — 桌面壳协议 2：配置通道、几何应用与远程浮层页

**What to build:** 桌面壳升级到协议 2。握手协商改为返回协议 2（旧 Web 声明 1-1 时返回 1，新 Web 声明 1-2 时返回 2），协商结果为协议 1 时浮层整体禁用（set-enabled 无效、不创建窗口）。新增 `voice-overlay:set-config` 通道（信任校验与既有通道一致），解析校验全量浮层配置（范围 clamp）：壳消费几何部分——浮层窗口按显示大小等比缩放（基准 280×300）、窗口中心点按 X/Y 百分比定位（50/50 居中）、窗口高度自适应成员行数（无上限）；样式部分经浮层窗口本地通道新增 `push-config` 转发（`get-state` 顺带返回配置）。浮层窗口改为加载服务器 Origin 下的浮层页面，不再打包本地 overlay 资源，CSP 收紧、只暴露受信任的浮层 preload 通道。

**Blocked by:** 02 — 桌面端浮层窗口与桥接收；03 — Web 端开关与状态推送

**Status:** resolved

- [ ] hello 协商返回协议 2；协议 1 请求（1-1）返回 1 且浮层禁用
- [ ] `set-config` 通道接收并校验配置，非法值 clamp 或拒绝
- [ ] 浮层窗口按配置应用大小缩放、中心点百分比位置、行数自适应高度
- [ ] `push-config` 转发样式配置，`get-state` 返回配置
- [ ] 浮层窗口加载远程浮层页面（mock 服务器验证），本地资源移除
- [ ] e2e 覆盖：协商、配置应用（大小/位置/高度）、旧 Web 禁用
