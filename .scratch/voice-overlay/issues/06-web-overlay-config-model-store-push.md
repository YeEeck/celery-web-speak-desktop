# 06 — Web 端浮层配置：模型、存储与推送

**What to build:** Web 端浮层配置模型：显示大小（默认 100%）、位置 X/Y（默认按现状换算：X 约 1%、Y 50%，中心点语义）、说话不透明度（默认 80%）、未说话不透明度（默认 40%）。配置持久化到 localStorage（与开关同一偏好体系）。协商结果为协议 2 时经 `set-config` 推送配置，拖动高频变化以约 50ms 节流合并、实时生效；协商为协议 1 或无桥时配置能力禁用（不发送配置，UI 依据协商结果隐藏）。

**Blocked by:** 05 — 桌面壳协议 2：配置通道、几何应用与远程浮层页

**Status:** resolved

- [ ] 配置模型带默认值，读写 localStorage 持久化
- [ ] 协议 2 时推送 `set-config`，节流 50ms 合并高频变化
- [ ] 协议 1/无桥时配置能力禁用，不发送配置
- [ ] 单测覆盖：存储、节流、协商降级
